# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Outputs (regenerable synthetic smoke-test artefacts). Keep the folder, ignore contents.
outputs/**
!outputs/.gitkeep

# OS
.DS_Store

# Real data mounts must NEVER be committed (place on EC2 only).
# Anchored to the repo root so the src/aegis_agency/data/ package is still tracked.
/data/
*.ckpt
*.safetensors
*.bin

# Vietnamese translation (local only — NEVER commit)
README.vi.md
docs/vi/
manuscript_vi/
