# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Outputs (regenerable synthetic smoke-test artefacts). Keep the folder, ignore contents.
outputs/**
!outputs/.gitkeep

# OS
.DS_Store

# Benchmark CSVs ship with the repo (data/benchmarks/ is now tracked so the
# dataset rides along to the server). Only skip binary weight/model files.
*.ckpt
*.safetensors
*.bin

# Vietnamese translation (local only — NEVER commit)
README.vi.md
docs/vi/
manuscript_vi/
